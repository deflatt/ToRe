#include <functional>
#include <iostream>
#include <Windows.h>

std::string ConvertToString(std::wstring wstr) {
    int size = WideCharToMultiByte(CP_UTF8, 0)
}

int main() {
    
    

    return 0;
}